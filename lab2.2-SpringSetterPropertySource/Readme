Assignment : Try with different scopes

Note : @Scope("prototype") is a replacement for Java's new operator:


Assignment : Create two Account beans with different name



@PropertySources({
    @PropertySource("classpath:foo.properties"),
    @PropertySource("classpath:bar.properties")
})


Profile names can be passed as jvm parameter
-Dspring.profiles.active=dev

in application.properties
spring.profiles.active=dev


In test environment
@ActiveProfiles("dev")